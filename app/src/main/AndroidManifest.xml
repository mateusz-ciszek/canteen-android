<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.microtemp.microblog">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".activity.menu.list.MenuListsActivity" />
        <activity android:name=".deletion.AdminActivity" />
        <activity android:name=".deletion.AddDishActivity" />
        <activity android:name=".deletion.TypeDishActivity" />
        <activity android:name=".deletion.FoodListActivity" />
        <activity android:name=".deletion.AdditionsActivity" />
        <activity android:name=".deletion.CurrentOrderActivity" />
        <activity android:name=".activity.menu.MenuActivity" />
        <activity android:name=".activity.foodDetails.FoodDetailsActivity" />
        <activity android:name=".activity.administration.dashboard.AdminDashboardActivity" />
        <activity android:name=".activity.administration.order.details.OrderDetailsActivity" />
        <activity android:name=".activity.administration.order.list.OrdersListActivity" />
        <activity android:name=".activity.cart.OrderCartActivity" />
        <activity android:name=".activity.administration.menu.list.MenusListManagementActivity" />
    </application>

</manifest>