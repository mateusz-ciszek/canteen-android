<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.canteen.app">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.canteen.app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.canteen.app.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.canteen.app.LoginActivity" />
        <activity android:name="com.canteen.app.RegisterActivity" />
        <activity android:name="com.canteen.app.activity.menu.list.MenuListsActivity" />
        <activity android:name="com.canteen.app.deletion.AddDishActivity"/>
        <activity android:name="com.canteen.app.activity.menu.MenuActivity" />
        <activity android:name="com.canteen.app.activity.foodDetails.FoodDetailsActivity" />
        <activity android:name="com.canteen.app.activity.administration.dashboard.AdminDashboardActivity" />
        <activity android:name="com.canteen.app.activity.administration.order.details.OrderDetailsActivity" />
        <activity android:name="com.canteen.app.activity.administration.order.list.OrdersListActivity" />
        <activity android:name="com.canteen.app.activity.cart.OrderCartActivity" />
        <activity android:name="com.canteen.app.activity.administration.menu.list.MenusListManagementActivity" />
        <activity android:name="com.canteen.app.activity.administration.menu.list.AddMenuActivity" />
        <activity android:name="com.canteen.app.activity.administration.menu.list.list.FoodListActivityAdmin" />
        <activity android:name="com.canteen.app.activity.foodDetails.FoodDetailsActivityAdmin" />
    </application>

</manifest>